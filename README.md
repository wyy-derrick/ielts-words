# 雅思背词 — 错词表 + 538 考点词 + 听力听写

纯前端背词工具，一个页面包含五个词库，托管在 GitHub Pages，
电脑 / iPad / 手机浏览器打开即用，无需服务器。
`d词表.xlsx` 只留在本地不上传，生成的词库数据会写入 `data.js`，网页端也能用。

在线访问：`https://<你的用户名>.github.io/<仓库名>/`

## 功能

- **词库切换**
  - **错词表**：阅读/听力复盘错词（来自 `../plan/雅思错词表.xlsx`，会持续更新），所有单词在一起，每满 100 词自动成一组
  - **538 考点词**：刘洪波考点词 378 个（固定词库，不再更新），保留原三组分类
  - **听力听写**：来自 xlsx「听力错词表」，播放英音后输入拼写
  - **d阅读错词**：来自本目录 `d词表.xlsx` 的阅读列，独立板块，题型和错词表一样（看英文选中文）
  - **d听力单词**：来自本目录 `d词表.xlsx` 的听力列，独立板块，题型和听力听写一样（听英音拼写）
- **题型自动适配**：错词表选中文释义；538 考点词选同义替换；听力听写听音拼写
- **答案卡**：释义 / 全部同义词 / 中文 / 雅思命题说明 / 记录来源 / 例句，有啥显示啥
- **背诵模式**：选择题显示单词后 4 选 1；听力听写先播英音再输入拼写，然后出答案卡
- **测试模式**：整组乱序测试 → 成绩单 + 错题本，支持"只测错题"重测
- **搜索**：按单词、中文、同义词搜全部词条
- **进度记忆**：背诵位置和错题次数保存在浏览器本地，三个词库进度独立；不同设备各自独立

## 日常更新词库（错词表有新词时）

```bash
# 1. xlsx 有新错词后（由 plan/生成雅思错词表.py 维护），在本目录运行：
/home/wyy/anaconda3/bin/python 更新词库.py

# 2. 推送，GitHub Pages 自动更新：
git add -A
git commit -m "更新词库"
git push
```

脚本读取 xlsx 中所有"错词表"工作表（阅读 + 听力），同一单词重复记错只保留
最早一条；已有单词的编号和顺序保持不变，新词自动追加在末尾。
正常只在末尾追加不影响浏览器里存的进度；若在中间删改行，进度可能轻微错位。

## d词表（两个独立板块）

`d词表.xlsx` 已写入 `.gitignore`，不会进仓库；生成的单词会写入 `data.js`，GitHub Pages 上可以切换「d阅读错词」「d听力单词」。

```
阅读单词 | 阅读中文 | 听力单词 | 听力中文
```

- 阅读列 → 前端「d阅读错词」，必须填中文才能出选择题
- 听力列 → 前端「d听力单词」，可以只填单词
- 填好后运行 `python 更新词库.py`，再 `git push`，网页即可更新
- 不会并进「错词表」或「听力听写」

## 首次部署（GitHub Pages）

1. 在 GitHub 新建一个仓库（Public），不要勾选初始化 README
2. 本目录已 git init 并完成首次提交，只需关联远程并推送：
   ```bash
   git remote add origin git@github.com:<你的用户名>/<仓库名>.git
   git push -u origin main
   ```
3. 仓库页面 → Settings → Pages → Source 选 **Deploy from a branch**，
   Branch 选 **main** / **root**，Save
4. 等 1–2 分钟部署完成，用上面的网址访问
5. iPad：Safari 打开网址 → 分享 → **添加到主屏幕**，即可以 App 形式全屏使用

## 文件说明

```
├── index.html    # 主程序（界面 + 逻辑，唯一入口）
├── data.js       # 词库数据（更新词库.py 自动生成，勿手改）
├── 词库538.json   # 538 考点词源数据（固定，不更新）
├── 更新词库.py    # xlsx + 词库538.json + d词表.xlsx → data.js
├── d词表.xlsx     # 源表（gitignore，不上传）
├── .gitignore
└── README.md
```

更新词库.py 运行依赖 openpyxl（anaconda base 环境自带）；若移动了本文件夹，
脚本会自动按 `~/wyy_notes/知识库/08-个人发展/英语/plan/` 兜底寻找 xlsx。

## 技术

纯前端，零依赖。数据通过 `<script>` 标签加载，无需构建和服务器。

## 致谢

- 页面交互与 538 考点词词库参考了 [selfache/IELT-538](https://github.com/selfache/IELT-538) 项目，感谢原作者的整理与分享。

## License

MIT
